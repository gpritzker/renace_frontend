'use server'

// Placeholder — Renace no tiene endpoints de direcciones actualmente
export const getProvinces = async (): Promise<any[]> => []
export const getCities = async (_provinceId: number): Promise<any[]> => []
